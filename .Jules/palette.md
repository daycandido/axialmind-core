## 2024-08-30 - Added Skip to Main Content Link
**Learning:** Foundational accessibility features like skip links are critical on blank canvas applications before complex interactive elements are added.
**Action:** Always check for missing semantic landmarks (`<main>`, `<nav>`, `<header>`) and skip links before attempting component-specific micro-UX improvements.
